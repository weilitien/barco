[pytest]
markers = rat: rat cases
          fast: fast cases

log_format = %(asctime)s -- %(name)s:%(funcName)s[%(levelname)s]: %(message)s (%(filename)s:%(lineno)d
log_date_format = %Y-%m-%d %H:%M:%S
addopts = -s --html=reports/test_reports.html --junitxml=reports/junit_result.xml
